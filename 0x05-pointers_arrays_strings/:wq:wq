#include "main.h"
#include <stdio.h>

/**
 * swap_int - function that swaps the values of two integers
 *@b: integer swapped
 *@a: integer swapped2
 * main - check the code
 *
 * Return: Always 0.
 */

void swap_int(int *a, int *b);
{

	temp = *a;
	*a = *b;
	*b = temp;
}

