#include "main.h"

/**
 * main - program that prints the number of arguments passed into it
 * @argc: number of command line argument
 * @argv: array contining program command lin e arguments
 * Return: 0 success
 */

int main(int argc, __attribute__((unused)), char *argv[])
{
	printf("%d\n", argc - 1;

	return (0);
}
